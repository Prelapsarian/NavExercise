var HW = require('./Nav');

describe('Hello world', function () {
  it('says hello', function () {
  	expect(HW.helloWorld()).toEqual('Hello world!');
  });
});


// unit test to make sure JSON is being returned
describe('Check if JSON response is valid', function () {
  it('response returns status code 200', function () {
  });
});

// check if menu has navigation nodes
describe('Check if JSON response is valid', function () {
  it('response returns status code 200', function () {
  });
});

// check if hamburger menu is being opened on click.
describe('Check if hamburger menu is open', function () {
  it('response returns status code 200', function () {
  });
});